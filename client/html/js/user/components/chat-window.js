/** @jsx React.DOM */
var React = require('react');
var s = getStyles();

var ChatWindow = React.createClass({
  propTypes: {},
  render: function() {
    return (
      <div>
        ChatWindow
      </div>
    );
  }
});

function getStyles() {
  return {

  };
}

module.exports = ChatWindow;
