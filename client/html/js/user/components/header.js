/** @jsx React.DOM */
var React = require('react');
var s = getStyles();

var Header = React.createClass({
  propTypes: {},
  render: function() {
    return (
      <div>
        Header
      </div>
    );
  }
});

function getStyles() {
  return {

  };
}

module.exports = Header;
