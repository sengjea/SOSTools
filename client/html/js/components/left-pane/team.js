/** @jsx React.DOM */
var React = require('react');

var Team = React.createClass({
  propTypes: {},
  render: function() {
    return (
      <div>
        Team
      </div>
    );
  }
});

module.exports = Team;
