/** @jsx React.DOM */
var React = require('react');

var RightPane = React.createClass({
  propTypes: {},
  render: function() {
    return (
      <div>RightPane</div>
    );
  }
});

module.exports = RightPane;
